# Database
DATABASE_URL="file:./dev.db"

# Admin credentials - CHANGE THESE IN PRODUCTION
ADMIN_PASSWORD=changeme

# Auth secrets - generate your own with: openssl rand -base64 32
AUTH_SECRET="replace-with-your-own-secret"
NEXTAUTH_SECRET="replace-with-your-own-secret"

# Auth config
AUTH_TRUST_HOST=true
AUTH_URL="https://your-domain.com"  # Change to your actual domain or http://localhost:3000 for local dev
